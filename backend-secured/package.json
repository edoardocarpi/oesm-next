{
  "name": "oesm-backend",
  "version": "1.0.0",
  "description": "OESM — Backend API e pipeline ETL",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "pipeline:run": "node scripts/run-pipeline.js",
    "pipeline:backfill": "node scripts/backfill.js",
    "db:migrate": "node scripts/migrate.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.2.0",
    "node-fetch": "^3.3.2",
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
